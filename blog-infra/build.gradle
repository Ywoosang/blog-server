dependencies {
}

tasks.register('copyConfig', Copy) {
    from '../blog-config/blog-infra/'
    include "application*.yml"
    into 'src/main/resources'
}

processResources.dependsOn('copyConfig')

bootJar {
    enabled = false
}
